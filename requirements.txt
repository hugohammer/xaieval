# Pinned to the versions the code was developed and tested against.
# Loosen the pins only after re-running `python -m pytest tests -q`: two of the
# tests guard against library behaviour that changed silently between versions
# (LIME's regression-mode coefficient sign, TreeSHAP's output scale).
numpy>=1.24,<2.3
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
shap>=0.44
lime>=0.2.0.1
joblib>=1.3

# Testing only
pytest>=7.4
